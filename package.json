{
  "name": "moontale",
  "version": "0.1.0",
  "private": true,
  "description": "MoonTale creates personalized bedtime stories for children, combining reading, imagination and gentle language exposure.",
  "scripts": {
    "format": "prettier --write \"**/*.{html,css,js,json,md,yml,yaml}\"",
    "format:check": "prettier --check \"**/*.{html,css,js,json,md,yml,yaml}\""
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
